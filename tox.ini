[tox]
envlist = py312, py313, py314
min_version = 4.0
skip_missing_interpreters = true

[testenv]
deps =
    pytest>=8
    pytest-asyncio>=0.23
    pytest-cov>=4
    httpx>=0.24
commands =
    pytest pp/server/tests -v --tb=short --cov=pp.server --cov-report=term-missing
